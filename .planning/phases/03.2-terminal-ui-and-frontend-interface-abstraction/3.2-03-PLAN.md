---
phase: 3.2-terminal-ui-and-frontend-interface-abstraction
plan: 03
type: execute
wave: 3
depends_on: [3.2-01, 3.2-02]
files_modified:
  - Cargo.toml
  - src/main.rs
  - src/cli/mod.rs
  - src/cli/app.rs
  - src/cli/terminal.rs
  - src/cli/session.rs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "The interactive CLI no longer depends on reedline"
    - "ratatui becomes the terminal adapter over the shared runtime protocol"
    - "The CLI can display transcript, activity, and composer state concurrently"
    - "ratatui uses crossterm as the terminal backend for this phase"
    - "terminal lifecycle restoration is explicit and panic-safe"
  artifacts:
    - path: "Cargo.toml"
      provides: "ratatui-based dependency set"
      contains: "ratatui"
      contains: "crossterm"
    - path: "src/cli/app.rs"
      provides: "ratatui app state and rendering"
      contains: "struct CliApp"
    - path: "src/main.rs"
      provides: "application boot through the new CLI adapter"
      contains: "CliApp"
  key_links:
    - from: "src/cli/app.rs"
      to: "src/frontend/protocol.rs"
      via: "consumes FrontendEvent and emits FrontendCommand"
      pattern: "FrontendEvent"
    - from: "src/main.rs"
      to: "src/session/runtime.rs"
      via: "boot runtime then attach ratatui frontend"
      pattern: "SessionRuntime"
---

<objective>
Implement the ratatui terminal frontend and replace reedline in the main CLI path.

Purpose: Deliver the new user experience while keeping it as one adapter over the shared runtime protocol.

Output: A ratatui-powered CLI workbench with transcript, activity feed, composer, and status line.
</objective>

<context>
@Cargo.toml
@src/main.rs
@src/cli/session.rs
@.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/3.2-CONTEXT.md
@.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/3.2-01-PLAN.md
@.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/3.2-02-PLAN.md
@.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/3.2-REVIEWS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ratatui terminal adapter and app reducer</name>
  <files>Cargo.toml, src/cli/app.rs, src/cli/terminal.rs, src/cli/mod.rs</files>
  <read_first>
    - Cargo.toml
    - src/cli/mod.rs
    - src/frontend/protocol.rs
  </read_first>
  <behavior>
    - CLI renders multiple panes from `FrontendEvent` state
    - composer supports message submission
    - app state is testable without an actual terminal
    - terminal input uses an explicit crossterm-driven event loop architecture
  </behavior>
  <action>
Introduce the ratatui-based CLI modules and reducer/state model.

Implementation guidance:
- add `ratatui` and `crossterm` as the terminal backend dependency set for this phase
- keep rendering code separate from event/state reduction
- model transcript entries and activity log separately
- make keyboard handling emit `FrontendCommand`
- document the input-loop choice explicitly: a dedicated terminal event task feeds command messages to the runtime/UI reducer boundary
- evaluate whether a textarea helper crate is warranted for composer ergonomics before hand-rolling editing behavior

Write reducer-focused tests that verify:
- incoming assistant/tool events update the correct panes
- submit/exit key handling maps to the right commands
  </action>
  <verify>
    <automated>cargo test cli --lib</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` no longer needs `reedline`
    - `src/cli/app.rs` contains a ratatui-driven app state/reducer
    - UI command handling is independent from the runtime implementation
    - backend choice is explicit (`crossterm`)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Boot the runtime through the ratatui app</name>
  <files>src/main.rs, src/cli/session.rs</files>
  <read_first>
    - src/main.rs
    - src/cli/session.rs
    - src/session/runtime.rs
  </read_first>
  <action>
Replace the reedline-based boot path so `main.rs` starts the ratatui adapter, attaches it to `SessionRuntime`, and runs the session until user exit or fatal error.

Keep the existing provider/model/thinking flags working exactly as before.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <acceptance_criteria>
    - `Session::run()` no longer constructs or calls `Reedline`
    - startup metadata is rendered through the frontend event path
    - CLI still exits cleanly on user intent
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden terminal lifecycle and panic recovery</name>
  <files>src/cli/terminal.rs, src/main.rs, src/cli/app.rs</files>
  <read_first>
    - src/main.rs
    - src/cli/terminal.rs
    - src/cli/app.rs
  </read_first>
  <behavior>
    - raw mode and alternate-screen setup are centralized
    - panic, fatal error, and clean exit paths all restore the terminal
    - terminal lifecycle is testable or at least verifiable through focused smoke checks
  </behavior>
  <action>
Add an explicit terminal lifecycle abstraction that owns:
- entering and leaving raw mode
- alternate screen and mouse capture setup/teardown
- panic-safe restoration guards or equivalent drop-based cleanup

Ensure the CLI adapter uses this abstraction rather than scattering crossterm setup calls across multiple modules.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <acceptance_criteria>
    - terminal restoration logic is centralized
    - panic and fatal-error paths restore the terminal before exit
    - no raw terminal side effects remain hidden in unrelated modules
  </acceptance_criteria>
</task>

</tasks>

<done>
The harness ships with a ratatui CLI that feels like a session workbench rather than a single-line REPL, while the runtime remains reusable for future adapters.
</done>
