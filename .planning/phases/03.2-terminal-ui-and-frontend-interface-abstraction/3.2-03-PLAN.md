---
phase: 03.2-terminal-ui-and-frontend-interface-abstraction
plan: 03
type: execute
wave: 3
depends_on: [3.2-01, 3.2-02]
files_modified:
  - src/cli/app.rs
  - src/cli/session.rs
  - src/cli/terminal.rs
  - src/cli/mod.rs
  - tests/cli_integration_test.rs
autonomous: true
requirements: [SC-1, SC-3, SC-4, SC-5]
user_setup: []

must_haves:
  truths:
    - "The terminal opens in a single-column Claude Code-like layout with a compact empty state near the composer."
    - "All assistant, thinking, tool, reminder, and status content shares one scrollable timeline and long content remains reachable."
    - "The todo list stays pinned at the bottom and updates in place instead of duplicating in scrollback."
    - "Typed interrupt commands still travel through the frontend/runtime boundary after the UI rewrite."
    - "Exit, fatal-error, and panic-safe restoration paths leave the terminal usable."
  artifacts:
    - path: "src/cli/app.rs"
      provides: "Single-column reducer, grouped timeline blocks, and render tests"
      contains:
        - "TimelineBlock"
        - "TodoSnapshot"
        - "TestBackend"
    - path: "src/cli/session.rs"
      provides: "Runtime/UI loop wired for one scroll surface and one composer"
      contains:
        - "run_ui_loop"
        - "frontend_event_channel(64)"
        - "frontend_command_channel(32)"
    - path: "src/cli/terminal.rs"
      provides: "Centralized terminal lifecycle guard and input listener"
      contains:
        - "TerminalGuard"
        - "spawn_input_listener"
    - path: "tests/cli_integration_test.rs"
      provides: "Non-interactive smoke coverage for startup and terminal lifecycle"
      contains:
        - "harness_help_smoke"
  key_links:
    - from: "src/cli/app.rs"
      to: "src/frontend/protocol.rs"
      via: "Reducer consumes `call_id` and `TodoSnapshot` directly"
      pattern: "TodoSnapshot"
    - from: "src/cli/session.rs"
      to: "src/cli/terminal.rs"
      via: "Session loop draws through TerminalGuard and reads from the dedicated input task"
      pattern: "spawn_input_listener"
    - from: "tests/cli_integration_test.rs"
      to: "src/cli/terminal.rs"
      via: "Smoke checks cover startup and restore behavior without a live model call"
      pattern: "TerminalGuard"
---

<objective>
Replace the split-pane workbench with a single-column terminal UI and lock in the scroll and lifecycle behavior that failed UAT.

Purpose: Deliver the actual Claude Code-like UX requested in the updated context while preserving the headless runtime boundary and hardening overflow/restoration behavior.
Output: A unified ratatui timeline, pinned todo footer, single composer/status stack, and targeted regression tests for overflow and terminal restoration.
</objective>

<execution_context>
@/Users/wangyue/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wangyue/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/3.2-CONTEXT.md
@.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/03.2-RESEARCH.md
@.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/03.2-UAT.md
@src/cli/app.rs
@src/cli/session.rs
@src/cli/terminal.rs
@src/frontend/protocol.rs
@.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/3.2-01-PLAN.md
@.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/3.2-02-PLAN.md

<interfaces>
Expected event surface from plans `3.2-01` and `3.2-02`:
```rust
pub enum FrontendEvent {
    SessionStarted { provider: String, model: String },
    UserMessageCommitted { turn_id: u64, text: String },
    AssistantMessageDelta { turn_id: u64, delta: String },
    AssistantMessageCompleted { turn_id: u64, text: String },
    Thinking { turn_id: u64, text: String },
    ToolCallStarted { turn_id: u64, call_id: String, name: String, args_preview: String },
    ToolCallFinished { turn_id: u64, call_id: String, name: String, result_preview: String },
    RetryScheduled { turn_id: u64, attempt: u32, max_retries: u32, delay_ms: u64, reason: String },
    Reminder { turn_id: u64, message: String },
    Error { turn_id: u64, message: String },
    TodoSnapshot { turn_id: u64, items: Vec<FrontendTodoItem> },
    Status { message: String },
    SessionEnded { summary: FrontendSessionSummary },
}
```

Current UI state that must be removed:
```rust
struct CliApp {
    transcript: Vec<TranscriptEntry>,
    activity: Vec<String>,
    focus: PaneFocus,
    transcript_scroll: PaneScrollState,
    activity_scroll: PaneScrollState,
    composer_scroll: PaneScrollState,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild `CliApp` around one timeline and a pinned todo footer</name>
  <files>src/cli/app.rs, src/cli/mod.rs</files>
  <read_first>
    - .planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/3.2-CONTEXT.md
    - .planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/03.2-UAT.md
    - src/cli/app.rs
    - src/cli/mod.rs
    - src/frontend/protocol.rs
  </read_first>
  <behavior>
    - CLI follows D-06 through D-10 instead of the split-pane workbench.
    - Tool results are attached to the matching tool-call block by `call_id`.
    - Todo snapshots update footer state in place and never append duplicate transcript entries.
    - Keyboard handling preserves `FrontendCommand::Interrupt` as a first-class command for SC-3.
  </behavior>
  <action>
    Replace the split-pane reducer in `src/cli/app.rs` with this exact state model:
    - `Vec<TimelineBlock>` where at minimum there are block variants for user message, assistant message, thinking, reminder/error/status note, and tool block
    - tool blocks store `{ turn_id: u64, call_id: String, name: String, args_preview: String, result_preview: Option<String> }`
    - one `ViewportState { offset: u16, follow_tail: bool }`
    - `todo_footer: Vec<FrontendTodoItem>`
    - `composer: String`
    - `status: String`
    - `provider: Option<String>` / `model: Option<String>`

    Update event reduction rules exactly:
    - `ToolCallStarted` appends a tool block.
    - `ToolCallFinished` locates the existing block with the same `call_id` and fills `result_preview`.
    - `TodoSnapshot` replaces `todo_footer` and does not append any timeline block.
    - `AssistantMessageDelta` updates the current streaming assistant state for the active turn.

    Remove `PaneFocus`, `TranscriptEntry`, `activity`, `transcript_scroll`, `activity_scroll`, and `composer_scroll`.
    Remove Tab/BackTab focus switching entirely; keep `Enter`, `Esc`, `Ctrl+C`, `Up`, `Down`, `PageUp`, `PageDown`, `Home`, and `End`.
    Map `Ctrl+C` to `FrontendCommand::Interrupt` and keep `Esc` mapped to `FrontendCommand::Exit`.

    Add reducer tests in `src/cli/app.rs` that prove:
    - tool start/finish events with the same `call_id` collapse into one grouped block
    - a second `TodoSnapshot` replaces the footer contents instead of duplicating timeline content
    - `Ctrl+C` yields `FrontendCommand::Interrupt` while `Esc` yields `FrontendCommand::Exit`
  </action>
  <verify>
    <automated>cargo test tool_group_and_todo_footer --lib</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/app.rs` defines `TimelineBlock`.
    - `src/cli/app.rs` handles `FrontendEvent::TodoSnapshot`.
    - `src/cli/app.rs` contains no `PaneFocus`.
    - `src/cli/app.rs` contains no `TranscriptEntry`.
    - `src/cli/app.rs` contains no `KeyCode::Tab`.
    - `src/cli/app.rs` maps `KeyCode::Char('c')` with `KeyModifiers::CONTROL` to `FrontendCommand::Interrupt`.
    - `src/cli/app.rs` maps `KeyCode::Esc` to `FrontendCommand::Exit`.
    - reducer tests cover grouped tool blocks and in-place todo footer updates.
  </acceptance_criteria>
  <done>
    The reducer owns one timeline plus one pinned footer, matching the updated UX model and eliminating split-pane assumptions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the split-pane render tree with a single-column layout and unified viewport</name>
  <files>src/cli/app.rs, src/cli/session.rs</files>
  <read_first>
    - .planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/03.2-RESEARCH.md
    - .planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/03.2-UAT.md
    - src/cli/app.rs
    - src/cli/session.rs
    - src/frontend/protocol.rs
  </read_first>
  <behavior>
    - Empty state stays visually close to the composer per D-06 and D-07.
    - Overflowing content remains reachable through one shared scroll surface per the UAT gap.
    - Session loop keeps D-11's dedicated input task and stable CLI entry behavior from D-05.
    - Session loop forwards typed interrupt commands to the runtime boundary.
  </behavior>
  <action>
    Rebuild `CliApp::render` and session-loop status text around this exact layout:
    - top-level vertical stack: `banner` height `1`, `display` fill, `composer` height `3`, `status` height `1`
    - banner text literal: `Harness`
    - inside `display`, split vertically into `timeline` and `todo footer`
    - when `todo_footer` is empty, footer height is `0`
    - when `todo_footer` is non-empty, footer height is the rendered todo text height plus borders
    - use `Flex::End` or equivalent bottom alignment so a short timeline hugs the composer instead of leaving a large blank pane

    Use one shared wrapped-line counting helper for timeline and footer math. Recompute max scroll on every draw and clamp the offset after terminal resize so long content never becomes unreachable.

    Update `src/cli/session.rs` so:
    - there is exactly one user-visible scroll surface
    - status/help copy references the single timeline instead of focus switching
    - `frontend_event_channel(64)` and `frontend_command_channel(32)` stay in place
    - `spawn_input_listener` remains the only terminal input source
    - `FrontendCommand::Interrupt` is sent through `send_frontend_command` to the runtime loop and is not rewritten to `Exit` inside the UI layer
  </action>
  <verify>
    <automated>cargo test single_column_layout --lib</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/app.rs` renders a top-level banner with `Harness`.
    - `src/cli/app.rs` contains one viewport state instead of separate pane scroll states.
    - `src/cli/app.rs` contains no pane titles named `Transcript`, `Activity`, or `Composer`.
    - `src/cli/session.rs` contains no `BackTab`.
    - `src/cli/session.rs` still sends `FrontendCommand::Interrupt` through the runtime command channel.
    - render/layout code clamps scroll after resize or max-scroll recomputation.
  </acceptance_criteria>
  <done>
    The visible terminal hierarchy is banner -> unified display -> composer -> status, and overflow no longer disappears behind pane boundaries.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add targeted render, lifecycle, and smoke coverage for the rebuilt terminal UI</name>
  <files>src/cli/app.rs, src/cli/terminal.rs, tests/cli_integration_test.rs</files>
  <read_first>
    - .planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/03.2-UAT.md
    - src/cli/app.rs
    - src/cli/terminal.rs
    - tests/cli_integration_test.rs
  </read_first>
  <behavior>
    - Verification stays protocol-first with targeted `TestBackend` assertions instead of brittle snapshot dumps.
    - Terminal restoration remains centralized per D-12.
    - Startup and teardown can be checked non-interactively in CI.
    - Exit, runtime error, panic, and constructor/early-failure restoration paths are all covered explicitly for SC-5.
  </behavior>
  <action>
    Add focused tests that cover the exact regressions and must-haves for this plan:
    - in `src/cli/app.rs`, use `ratatui::backend::TestBackend` to assert the compact empty state, pinned todo footer placement, and long-content scroll/resize clamp behavior
    - in `src/cli/terminal.rs`, keep `TerminalGuard` as the single raw-mode / alternate-screen owner and add explicit tests for idempotent restore, panic-path restore, and constructor rollback when `TerminalGuard::new()` fails after raw mode or screen setup has partially succeeded
    - make `TerminalGuard::new()` restore raw mode / alternate screen state on every early-return failure path instead of assuming construction either fully succeeds or does nothing
    - in `tests/cli_integration_test.rs`, keep `harness --help` smoke and add non-interactive startup/teardown coverage for clean exit plus one runtime-error path that confirms terminal restoration without requiring a live model call

    Do not reintroduce pane-title snapshot tests or live-provider dependencies.
  </action>
  <verify>
    <automated>cargo test single_column_layout --lib && cargo test --test cli_integration_test && cargo clippy --all-targets --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/app.rs` tests reference `TestBackend`.
    - `src/cli/app.rs` tests assert overflow or resize-clamp behavior.
    - `src/cli/terminal.rs` still defines `TerminalGuard`.
    - `src/cli/terminal.rs` tests cover panic restore.
    - `src/cli/terminal.rs` tests cover `TerminalGuard::new()` early-failure rollback.
    - `tests/cli_integration_test.rs` contains `harness_help_smoke`.
    - automated tests exercise clean exit restore and runtime-error restore without a live model request.
  </acceptance_criteria>
  <done>
    The new single-column UI is protected against the exact overflow and restoration regressions surfaced by the updated phase context and UAT.
  </done>
</task>

</tasks>

<verification>
Run the targeted library tests, the CLI integration test binary, and the full clippy gate. Confirm the code no longer contains split-pane focus concepts and that the new tests cover overflow, todo pinning, and terminal restore behavior.
</verification>

<success_criteria>
The shipped ratatui adapter matches the updated single-column design, preserves headless runtime boundaries, and includes explicit regression coverage for the overflow bug that blocked verification.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/03.2-03-SUMMARY.md`
</output>
