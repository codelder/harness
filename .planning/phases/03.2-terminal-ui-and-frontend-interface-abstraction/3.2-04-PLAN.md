---
phase: 3.2-terminal-ui-and-frontend-interface-abstraction
plan: 04
type: execute
wave: 4
depends_on: [3.2-03]
files_modified:
  - Cargo.toml
  - src/main.rs
  - src/cli/app.rs
  - src/cli/session.rs
  - src/agent/loop_.rs
  - tests/agent_loop_test.rs
  - tests/cli_integration_test.rs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "The migration preserves core session behavior while removing legacy reedline assumptions"
    - "The new runtime/frontend split has regression coverage for exit, errors, and event delivery"
    - "The codebase no longer carries stale reedline-only paths"
    - "Behavioral equivalence for nag reminders, tool activity, and graceful exit is explicitly tested"
  artifacts:
    - path: "tests/cli_integration_test.rs"
      provides: "integration coverage for runtime/frontend coordination"
      contains: "SessionRuntime"
      contains: "SessionStarted"
    - path: "Cargo.toml"
      provides: "dependency cleanup after migration"
      contains: "ratatui"
    - path: "src/cli/session.rs"
      provides: "finalized adapter/runtime wiring without legacy reedline control flow"
      contains: "FrontendCommand"
  key_links:
    - from: "tests/cli_integration_test.rs"
      to: "src/session/runtime.rs"
      via: "behavioral verification"
      pattern: "submit"
    - from: "Cargo.toml"
      to: "src/cli/app.rs"
      via: "final dependency graph"
      pattern: "ratatui"
---

<objective>
Complete migration cleanup and verification for the new frontend architecture.

Purpose: Ensure the ratatui migration does not regress the validated behavior from earlier phases and leaves a clean base for future web/desktop/social adapters.

Output: Regression coverage, dependency cleanup, and finalized runtime/frontend boundaries.
</objective>

<context>
@Cargo.toml
@tests/agent_loop_test.rs
@.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/3.2-CONTEXT.md
@.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/3.2-03-PLAN.md
@.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/3.2-REVIEWS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add integration coverage for runtime/frontend coordination</name>
  <files>tests/cli_integration_test.rs, tests/agent_loop_test.rs</files>
  <read_first>
    - tests/agent_loop_test.rs
    - src/session/runtime.rs
    - src/frontend/protocol.rs
  </read_first>
  <behavior>
    - session startup emits expected metadata events
    - successful submit updates transcript flow correctly
    - exit/interrupt paths produce deterministic summaries/errors
    - migration preserves nag reminder and tool-activity visibility semantics
  </behavior>
  <action>
Add focused tests that exercise the new runtime/frontend boundary without requiring a real terminal.

Favor reducer/runtime tests over brittle terminal snapshot tests. Verify protocol correctness and lifecycle transitions instead of pixel-perfect layout.

Explicitly cover:
- multi-turn conversation flow
- tool activity/event propagation
- nag reminder injection behavior
- graceful exit and interrupt summaries
- a lightweight startup smoke path such as `harness --help`
- one non-interactive ratatui lifecycle smoke, such as terminal-guard boot/teardown or fake-terminal restore verification
  </action>
  <verify>
    <automated>cargo test</automated>
  </verify>
  <acceptance_criteria>
    - runtime/frontend integration tests exist
    - core library test suite still passes
    - exit and error paths are covered
    - nag reminder and tool-activity behaviors are regression-tested
    - at least one smoke check exercises terminal lifecycle setup/restore without requiring a live model call
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove stale reedline-era assumptions and finalize migration</name>
  <files>Cargo.toml, src/main.rs, src/cli/session.rs, src/agent/loop_.rs</files>
  <read_first>
    - Cargo.toml
    - src/cli/session.rs
    - src/main.rs
  </read_first>
  <action>
Finish the migration by:
- removing unused `reedline` dependencies/imports
- deleting obsolete terminal-specific output paths
- ensuring the binary still reports fatal startup/runtime errors clearly
- tightening any remaining naming/API inconsistencies from the extraction
- documenting any residual non-equivalence if a behavior intentionally changed
  </action>
  <verify>
    <automated>cargo clippy --all-targets --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `reedline` is removed from `Cargo.toml`
    - no dead legacy CLI path remains
    - lint/test suite is green or all remaining issues are documented
    - CLI startup smoke verification exists
  </acceptance_criteria>
</task>

</tasks>

<done>
Phase 3.2 lands as a clean architectural migration instead of a one-off TUI rewrite, and the repository is ready to add future frontends on the same protocol.
</done>
