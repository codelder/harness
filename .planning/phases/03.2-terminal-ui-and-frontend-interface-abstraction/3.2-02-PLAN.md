---
phase: 3.2-terminal-ui-and-frontend-interface-abstraction
plan: 02
type: execute
wave: 2
depends_on: [3.2-01]
files_modified:
  - src/agent/loop_.rs
  - src/agent/mod.rs
  - src/llm/provider.rs
  - src/session/runtime.rs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "agent-level observability is emitted as structured events rather than direct terminal writes"
    - "tool call, tool result, thinking, retry, and assistant text flow can be forwarded to any frontend"
    - "TodoUsageHook remains functionally correct while no longer coupling to stderr"
    - "PromptHook uses an explicit side channel to escape structured events"
    - "Retry notifications are surfaced through runtime-observable metadata, not terminal writes"
  artifacts:
    - path: "src/agent/loop_.rs"
      provides: "hook-driven event forwarding"
      contains: "FrontendEvent"
      contains: "event_tx"
    - path: "src/session/runtime.rs"
      provides: "runtime handling of agent execution events"
      contains: "ToolCallStarted"
      contains: "AssistantMessageCompleted"
    - path: "src/llm/provider.rs"
      provides: "provider invocation path compatible with runtime observers"
      contains: "chat_with_history_and_hook"
  key_links:
    - from: "src/agent/loop_.rs"
      to: "src/frontend/protocol.rs"
      via: "structured event emission"
      pattern: "FrontendEvent"
    - from: "src/session/runtime.rs"
      to: "TodoUsageHook"
      via: "hook wired through runtime-owned observer"
      pattern: "TodoUsageHook"
---

<objective>
Refactor agent execution observability into structured events.

Purpose: Make the runtime/frontends observe the agent in real time without baking terminal behavior into the agent or provider layers.

Output: The hook/retry path emits typed events that the runtime can forward to ratatui now and web/desktop adapters later.
</objective>

<context>
@.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/3.2-CONTEXT.md
@src/agent/loop_.rs
@src/agent/mod.rs
@src/llm/provider.rs
@src/cli/session.rs
@.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/3.2-REVIEWS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace direct stderr writes in TodoUsageHook and retry flow</name>
  <files>src/agent/loop_.rs, src/agent/mod.rs</files>
  <read_first>
    - src/agent/loop_.rs
    - src/frontend/protocol.rs
  </read_first>
  <behavior>
    - hook still tracks todo usage correctly
    - tool/thinking/retry information is surfaced via typed events
    - agent layer no longer assumes a terminal is present
    - the design is compatible with rig-core `PromptHook` trait constraints
  </behavior>
  <action>
Refactor `TodoUsageHook` (or replace it with a more general observer hook) so that:
- tool call start/result, thinking content, retry notices, and optional text deltas become structured runtime events
- no `print!`, `println!`, `eprint!`, or `eprintln!` remain in `src/agent/loop_.rs`
- assistant final text is still returned as the canonical completed response
- the hook receives an explicit side channel (event sender / sink) in addition to the todo-usage flag
- the design documents how events escape `PromptHook` methods without changing rig-core trait contracts

Preserve direct todo detection semantics while decoupling display concerns.
  </action>
  <verify>
    <automated>cargo test agent --lib</automated>
  </verify>
  <acceptance_criteria>
    - `src/agent/loop_.rs` contains no direct terminal writes
    - retry notifications can be observed by the runtime
    - todo usage detection remains direct and hook-based
    - the hook/event API is explicit enough to guide `src/llm/provider.rs` signature changes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire the runtime to consume and forward agent events</name>
  <files>src/session/runtime.rs, src/llm/provider.rs</files>
  <read_first>
    - src/session/runtime.rs
    - src/llm/provider.rs
  </read_first>
  <action>
Update the runtime/provider boundary so runtime-owned event sinks can observe:
- startup/session metadata
- assistant output lifecycle
- tool activity
- error/retry transitions

The provider API may keep using hooks internally, but the runtime should be the component that translates those hook callbacks into `FrontendEvent`s for downstream adapters.

Make the provider signature changes explicit:
- runtime constructs the hook with todo flag + event sink
- provider accepts the richer hook or observer object without losing multi-provider support
- `with_retry()` receives a notifier callback or equivalent metadata path instead of writing directly to the terminal
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <acceptance_criteria>
    - runtime can forward hook activity without depending on a terminal library
    - `src/llm/provider.rs` still supports the existing providers
    - no new blocking primitives are introduced
    - retry events are observable without embedding output concerns into generic helpers
  </acceptance_criteria>
</task>

</tasks>

<done>
Agent execution is observable through a reusable event stream instead of terminal side effects, unlocking richer TUIs and future remote/front-channel adapters.
</done>
