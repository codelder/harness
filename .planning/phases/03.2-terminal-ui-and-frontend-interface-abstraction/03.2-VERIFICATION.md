---
phase: 03.2-terminal-ui-and-frontend-interface-abstraction
verified: 2026-03-26T03:50:57Z
status: human_needed
score: 5/5 must-haves verified
human_verification:
  - test: "Claude Code-like single-column terminal feel"
    expected: "Banner shows `Harness`, the empty state sits close to the composer, and no split-pane chrome or focus-switch mental model remains."
    why_human: "Automated render assertions verify structure and spacing bounds, but not subjective terminal feel or visual hierarchy in a live TTY."
  - test: "Grouped tool-call/result readability with pinned todo footer"
    expected: "A real interactive run shows tool calls and results as one grouped block in the unified stream while the todo footer stays pinned below the timeline."
    why_human: "Reducer and render tests prove pairing and footer placement, but live-terminal readability and scanning quality still need human judgement."
---

# Phase 3.2: Terminal UI and Frontend Interface Abstraction Verification Report

**Phase Goal:** Replace the split-pane ratatui workbench with a Claude Code-like single-column terminal UI and a corrected shared frontend protocol that can later support CLI, web, desktop, and social adapters.
**Verified:** 2026-03-26T03:50:57Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | The interactive terminal experience uses a single-column Claude Code-like layout with banner, unified timeline, composer, and status. | ✓ VERIFIED | `CliApp::render` uses a vertical `banner -> display -> composer -> status` layout, a `Harness` banner, bottom-aligned empty state, and one viewport in `src/cli/app.rs`; render tests cover compact empty state and footer placement. |
| 2 | The agent loop and session runtime no longer depend directly on CLI-specific input/output primitives. | ✓ VERIFIED | `SessionRuntime` emits typed frontend events and owns todo/session state without `println!`, `eprintln!`, `ratatui`, or `crossterm`; provider calls stay behind `chat_with_history_and_hook`. |
| 3 | A shared frontend/session protocol represents streaming output, tool activity, interrupts, lifecycle events, and event identity. | ✓ VERIFIED | `src/frontend/protocol.rs` defines typed commands/events with `turn_id`, `call_id`, todo snapshots, must-deliver vs best-effort delivery, and channel helpers; protocol tests pass. |
| 4 | Tool call/result groups stay paired in the unified stream and the todo list stays pinned at the bottom with in-place updates. | ✓ VERIFIED | `CliApp` matches tool results by `(turn_id, call_id)`, `TodoSnapshot` replaces footer state without appending timeline rows, and reducer tests cover out-of-order completion, duplicate `call_id`s across turns, and footer replacement. |
| 5 | Overflowing content remains reachable and terminal exit/error/panic paths restore the shell cleanly. | ✓ VERIFIED | Viewport math recomputes/clamps scroll during render, render tests cover overflow/resize, and `TerminalGuard` plus integration smokes cover clean exit, runtime error, constructor rollback, and panic restore behavior. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/frontend/protocol.rs` | Typed shared protocol with turn identity, tool correlation, todo snapshots, and bounded channel semantics | ✓ VERIFIED | Defines `SESSION_START_TURN_ID`, `FrontendTodoItem`, `FrontendTodoStatus`, `ToolCallStarted/Finished` with `turn_id` and `call_id`, `TodoSnapshot`, delivery modes, and backlog flush helpers. |
| `src/frontend/mod.rs` | Module re-exports for downstream consumers | ✓ VERIFIED | Re-exports protocol surface including todo types and `SESSION_START_TURN_ID`. |
| `src/lib.rs` | Crate-root export surface for shared protocol types | ✓ VERIFIED | Re-exports channel helpers, protocol types, todo types, and `SESSION_START_TURN_ID`. |
| `src/session/runtime.rs` | Headless runtime that emits typed lifecycle, transcript, reminder, error, and todo-footer events | ✓ VERIFIED | Starts with `SessionStarted` plus startup `TodoSnapshot`, emits turn-scoped submit/assistant/reminder/todo/error events, and ends with `SessionEnded`. |
| `src/agent/loop_.rs` | PromptHook observability with turn-scoped tool/retry/streaming events | ✓ VERIFIED | `TodoUsageHook::new(turn_id, event_tx)` emits `ToolCallStarted`, `ToolCallFinished`, `Thinking`, `AssistantMessageDelta`, and `RetryScheduled` with stable ids. |
| `src/planning/todo.rs` | Typed todo snapshot source for runtime mapping | ✓ VERIFIED | `TodoManager::snapshot()` returns cloned todo state for runtime-owned snapshots. |
| `src/cli/app.rs` | Single-column reducer, unified timeline, pinned footer, and render/reducer tests | ✓ VERIFIED | Contains `TimelineBlock`, one `ViewportState`, grouped tool blocks, footer replacement logic, key handling, render math, and targeted tests. |
| `src/cli/session.rs` | UI/runtime loop wiring with one event stream, one input source, and UI-owned best-effort flushing | ✓ VERIFIED | Uses `frontend_event_channel(64)`, `frontend_command_channel(32)`, `spawn_input_listener`, `TerminalGuard`, and `flush_best_effort()` from the UI loop only. |
| `src/cli/terminal.rs` | Centralized terminal lifecycle ownership and input listener | ✓ VERIFIED | `TerminalGuard` tracks raw-mode/alternate-screen state, restores on drop, and isolates crossterm input in `spawn_input_listener`. |
| `tests/cli_integration_test.rs` | Non-interactive startup/lifecycle smoke coverage | ✓ VERIFIED | Covers `--help`, startup/exit metadata, interrupt feedback, clean restore, and runtime-error restore without a live model call. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `src/session/runtime.rs` | `src/frontend/protocol.rs` | Runtime emits typed lifecycle and todo snapshot events | ✓ WIRED | `start_with_provider`, `submit_message_with`, `handle_command`, and `end` construct `FrontendEvent` variants directly, including `TodoSnapshot` and `SessionEnded`. |
| `src/session/runtime.rs` | `src/planning/todo.rs` | Runtime maps `TodoManager` state into frontend-safe snapshots | ✓ WIRED | `todo_snapshot()` calls `manager.snapshot()`, then `emit_todo_snapshot_items()` maps `TodoItem -> FrontendTodoItem`. |
| `src/agent/loop_.rs` | `src/frontend/protocol.rs` | PromptHook side channel emits call-aware tool events and turn-scoped streaming/retry events | ✓ WIRED | `TodoUsageHook` builds `FrontendEvent` values using `tool_call_id.unwrap_or_else(|| internal_call_id.to_string())`. |
| `src/llm/provider.rs` | `src/agent/loop_.rs` | Provider boundary forwards hook unchanged into rig prompt flow | ✓ WIRED | `chat_with_history_and_hook()` uses `.with_hook(hook)` for Anthropic/OpenAI without provider-specific event branches. |
| `src/cli/app.rs` | `src/frontend/protocol.rs` | Reducer consumes shared event protocol directly | ✓ WIRED | `apply_event()` handles `ToolCallStarted`, `ToolCallFinished`, `TodoSnapshot`, streaming deltas, reminders, status, errors, and lifecycle events. |
| `src/cli/session.rs` | `src/cli/terminal.rs` | Session loop draws through `TerminalGuard` and reads only from dedicated input task | ✓ WIRED | `Session::run()` creates `TerminalGuard`, spawns `spawn_input_listener()`, and passes both into `run_ui_loop()`. |
| `tests/cli_integration_test.rs` | `src/cli/terminal.rs` | Smoke checks verify restore behavior | ✓ WIRED | Integration tests call `terminal_restore_smoke()` and assert clean-exit/runtime-error cleanup sequences. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `src/session/runtime.rs` | `items` in `TodoSnapshot` | `TodoManager::snapshot()` from `src/planning/todo.rs`, mapped through `map_todo_item()` | Yes | ✓ FLOWING |
| `src/agent/loop_.rs` | `call_id`, `result_preview`, `delta`, `thinking`, `retry` event payloads | PromptHook callbacks from provider responses/tool calls | Yes | ✓ FLOWING |
| `src/cli/app.rs` | `timeline`, `streaming_assistant`, `todo_footer` | `FrontendEvent` stream from runtime/hook boundary | Yes | ✓ FLOWING |
| `src/cli/session.rs` | best-effort event delivery into reducer | Shared `FrontendEventSender` backlog flushed from UI loop | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Protocol contract compiles and tests its delivery semantics | `cargo test frontend::protocol --lib` | 4 tests passed | ✓ PASS |
| Runtime emits turn-scoped lifecycle/todo/tool observability without terminal I/O | `cargo test runtime --lib` | 14 tests passed | ✓ PASS |
| Reducer groups tool/result blocks and accepts pinned footer updates | `cargo test tool_group_and_todo_footer --lib` | 1 test passed | ✓ PASS |
| CLI build and smoke paths stay runnable | `cargo check` | Completed successfully | ✓ PASS |
| Startup/help and terminal restore smoke paths work without a live model call | `cargo test --test cli_integration_test` | 9 tests passed | ✓ PASS |

### Requirements Coverage

Repository note: `.planning/REQUIREMENTS.md` is absent in this repo. Requirement coverage was cross-checked against the phase requirement map in `.planning/ROADMAP.md` plus the `requirements:` fields in `3.2-01-PLAN.md`, `3.2-02-PLAN.md`, and `3.2-03-PLAN.md`.

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| `SC-1` | `3.2-03` | Single-column Claude Code-like terminal layout with compact empty state | ? NEEDS HUMAN | Single-column render tree, banner, bottom-aligned empty state, and layout tests are present in `src/cli/app.rs`; live-terminal feel still needs manual confirmation. |
| `SC-2` | `3.2-01`, `3.2-02` | Headless runtime remains independent from CLI-specific I/O | ✓ SATISFIED | `src/session/runtime.rs` and `src/agent/loop_.rs` emit typed events and contain no terminal UI dependencies; `src/llm/provider.rs` keeps provider/hook wiring generic. |
| `SC-3` | `3.2-01`, `3.2-02`, `3.2-03` | Shared protocol represents streaming, tool, retry, interrupt, and lifecycle events over typed channels | ✓ SATISFIED | `src/frontend/protocol.rs` defines the full typed command/event surface and delivery semantics; runtime and reducer consume it end-to-end. |
| `SC-4` | `3.2-01`, `3.2-02`, `3.2-03` | Tool results stay paired with their calls and todo state updates in place as a pinned footer | ? NEEDS HUMAN | Automated reducer/runtime tests prove pairing and in-place footer replacement; live-terminal readability of grouped blocks still needs human confirmation. |
| `SC-5` | `3.2-03` | Overflow, resize, exit, and panic/error paths keep content visible and restore the terminal cleanly | ✓ SATISFIED | Viewport clamp tests, `TerminalGuard` restore paths, constructor rollback tests, panic restore tests, and integration smoke coverage all pass. |

All phase requirement IDs named by the user (`SC-1` through `SC-5`) are present in the roadmap requirement map and are accounted for by the union of the three plan frontmatters. No orphaned phase requirement IDs were found.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `src/session/runtime.rs` | 171 | `"Interrupt requested but not yet implemented"` | ℹ️ Info | Interrupt is typed and propagated through the shared boundary, but this phase still stops at feedback/status rather than canceling in-flight work. |

### Human Verification Required

### 1. Claude Code-like Single-Column Feel

**Test:** Run `cargo run` in a real terminal and open the interactive UI.
**Expected:** `Harness` appears at the top, the empty state sits close to the composer, and there is no split-pane chrome or focus-switch behavior.
**Why human:** Buffer assertions confirm structure and spacing bounds, not subjective terminal feel.

### 2. Grouped Tool/Result Readability

**Test:** In a live session, trigger a tool call such as reading a file and then update todos.
**Expected:** The tool call and its result appear as one grouped block in the unified stream, and the todo footer remains pinned below the timeline with in-place updates.
**Why human:** Reducer tests prove correlation and footer replacement, but only a human can judge whether the rendered grouping is easy to scan in a real terminal.

### Gaps Summary

No blocking implementation gaps were found in the codebase for phase 03.2. The protocol, headless runtime, reducer, session loop, terminal lifecycle guard, and smoke coverage all exist, are wired together, and pass targeted verification. Remaining work is human-only confirmation of live-terminal visual feel and readability.

---

_Verified: 2026-03-26T03:50:57Z_
_Verifier: Claude (gsd-verifier)_
