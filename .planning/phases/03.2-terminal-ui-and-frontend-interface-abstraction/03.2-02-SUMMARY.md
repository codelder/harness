---
phase: 03.2-terminal-ui-and-frontend-interface-abstraction
plan: 02
subsystem: ui
tags: [rust, runtime, frontend-protocol, prompt-hook, todo, ratatui]
requires:
  - phase: 3.2-01
    provides: "Frontend protocol turn ids, call ids, and TodoSnapshot event types"
provides:
  - "Runtime-owned todo snapshot emission keyed to TodoManager state transitions"
  - "Turn-scoped PromptHook observability tests for tool, retry, thinking, and delta events"
  - "Typed runtime coverage for pre-turn submit errors and interrupt outcomes"
affects: [3.2-03, cli, frontend-protocol, runtime]
tech-stack:
  added: []
  patterns:
    [
      "turn-scoped frontend events over tokio::sync::mpsc",
      "runtime-owned TodoManager snapshot comparison",
    ]
key-files:
  created: []
  modified:
    [src/planning/todo.rs, src/session/runtime.rs, src/agent/loop_.rs]
key-decisions:
  - "Emit TodoSnapshot only when TodoManager state changes or reminders need the pinned footer, so tool observation alone does not churn the reducer."
  - "Align hook-focused test names with `cargo test runtime --lib` so the plan verification command exercises PromptHook observability."
patterns-established:
  - "Compare pre-turn and post-turn todo snapshots inside SessionRuntime before emitting footer updates."
  - "When plans verify with filtered cargo test names, keep targeted tests discoverable by that filter."
requirements-completed: [SC-2, SC-3, SC-4]
duration: 11min
completed: 2026-03-26
---

# Phase 3.2 Plan 02: Runtime Event Wiring Summary

**Turn-scoped runtime events with call-correlated tool observability and TodoManager-backed footer snapshots**

## Performance

- **Duration:** 11 min
- **Started:** 2026-03-26T03:08:48Z
- **Completed:** 2026-03-26T03:19:43Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Runtime todo footer updates now come from actual `TodoManager` state transitions instead of raw todo-tool observation.
- `cargo test runtime --lib` now exercises hook-side turn and call identity coverage as well as runtime interrupt/error behavior.
- The headless runtime still emits typed lifecycle events, reminder events, and clean session shutdown metadata through the shared boundary.

## Task Commits

Each task was committed atomically:

1. **Task 1: Emit turn-scoped todo snapshots from the headless runtime** - `113503b` (fix)
2. **Task 2: Thread `turn_id` and `call_id` through PromptHook and retry observability** - `fb09166` (test)

## Files Created/Modified

- `src/planning/todo.rs` - Added runtime-focused coverage for the clone-based snapshot accessor.
- `src/session/runtime.rs` - Gated `TodoSnapshot` emission on real todo state changes and added runtime command-path coverage.
- `src/agent/loop_.rs` - Added runtime-filtered PromptHook tests for call-id fallback and turn-scoped streaming/retry events.

## Decisions Made

- Compare todo snapshots before and after each successful turn so footer updates reflect runtime truth instead of tool usage side effects.
- Keep verification aligned with the plan's filtered cargo command by naming hook/runtime tests with the `runtime` substring.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `cargo fmt -- src/planning/todo.rs src/session/runtime.rs` reformatted clean files outside the task scope in this workspace. Those accidental changes were restored before committing so the task commits stayed atomic.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `SessionRuntime` now emits reducer-ready todo snapshots only when runtime state actually changes, which removes footer churn before the single-column CLI rebuild.
- PromptHook and runtime verification now cover turn identity, call correlation, retry observability, interrupt status, and pre-turn error routing needed by `3.2-03`.

## Self-Check: PASSED

- Found `.planning/phases/03.2-terminal-ui-and-frontend-interface-abstraction/03.2-02-SUMMARY.md`
- Found commit `113503b`
- Found commit `fb09166`
