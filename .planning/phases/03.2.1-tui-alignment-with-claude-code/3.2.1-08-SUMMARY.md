---
phase: 03.2.1-tui-alignment-with-claude-code
plan: 08
subsystem: session
tags: [token-usage, rig-core, prompt-response, statusline, frontend-events]

# Dependency graph
requires:
  - phase: 03.2.1
    provides: "FrontendEvent::TokenUsage protocol, CliApp statusline rendering, BestEffort coalescing"
provides:
  - "Token usage emission chain: LlmProvider -> AgentTurn -> SessionRuntime -> FrontendEvent::TokenUsage"
  - "Real token counts flowing from rig-core Usage through to statusline display"
affects: [03.2.1-verification, statusline-display]

# Tech tracking
tech-stack:
  added: []
  patterns: ["extended_details() for PromptResponse with Usage", "Option<Usage> propagation through agent layers"]

key-files:
  created: []
  modified:
    - "src/agent/message.rs"
    - "src/llm/provider.rs"
    - "src/agent/loop_.rs"
    - "src/session/runtime.rs"

key-decisions:
  - "Use extended_details() on PromptRequest to get PromptResponse with total_usage instead of plain String"
  - "Place TokenUsage emission between AssistantMessageCompleted and reminder check in submit_message_with"
  - "Use Option<Usage> on AgentTurn so Ollama (unimplemented) can return None gracefully"

patterns-established:
  - "Token usage propagation: LlmProvider returns (String, Option<Usage>) via extended_details(), agent_loop destructures into AgentTurn.usage"
  - "Best-effort event emission: TokenUsage uses BestEffort delivery mode for high-frequency tolerance"

requirements-completed: [REQ-3.2.1-07]

# Metrics
duration: 4min
completed: 2026-03-28
---

# Phase 03.2.1 Plan 08: Token Usage Emission Wiring Summary

**Wire rig-core Usage data through LlmProvider -> AgentTurn -> SessionRuntime -> FrontendEvent::TokenUsage so the statusline shows real token counts**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-27T18:49:10Z
- **Completed:** 2026-03-27T18:53:15Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Refactored LlmProvider::chat_with_history_and_hook to use extended_details() returning PromptResponse with Usage
- Added usage: Option<Usage> to AgentTurn and propagated through agent_loop
- SessionRuntime now emits FrontendEvent::TokenUsage after each turn with real token counts
- Removed the Token Usage Investigation comment block (no longer needed)
- New test proves the full emission chain: runtime_emits_token_usage_from_agent_turn

## Task Commits

Each task was committed atomically:

1. **Task 1: Wire token usage from rig-core through AgentTurn** - `8a4acdc` (feat)
2. **Task 2: Emit TokenUsage event from SessionRuntime** - `a6bddde` (feat)

**Plan metadata:** pending (docs: complete plan)

## Files Created/Modified
- `src/agent/message.rs` - Added usage: Option<Usage> field to AgentTurn struct
- `src/llm/provider.rs` - Refactored chat_with_history_and_hook to use extended_details() returning (String, Option<Usage>)
- `src/agent/loop_.rs` - Destructure provider result into (response_text, usage) and propagate into AgentTurn
- `src/session/runtime.rs` - Emit FrontendEvent::TokenUsage after AssistantMessageCompleted, removed investigation comment, added emission test

## Decisions Made
- Used extended_details() on PromptRequest to get PromptResponse with total_usage instead of plain String -- this is the canonical rig-core 0.31.0 API for token tracking
- Placed TokenUsage emission after AssistantMessageCompleted but before reminder check -- ensures event ordering is consistent and usage data is available before any todo processing
- Used Option<Usage> on AgentTurn so the Ollama stub (which doesn't return PromptResponse) can return None gracefully

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- REQ-3.2.1-07 (Enhanced Statusline) is now fully satisfied -- token counts flow from rig-core to statusline display
- The TUI alignment phase is complete with all 7 requirements verified
- No blockers or concerns

---
*Phase: 03.2.1-tui-alignment-with-claude-code*
*Completed: 2026-03-28*

## Self-Check: PASSED

- All 4 modified files verified present
- Commit 8a4acdc (Task 1) verified in git log
- Commit a6bddde (Task 2) verified in git log
- 148 library tests passing
