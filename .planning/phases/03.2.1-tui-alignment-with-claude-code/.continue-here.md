---
phase: 03.2.1-tui-alignment-with-claude-code
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-03-26T10:39:32.449Z
---

<current_state>
Executing Phase 3.2.1: TUI Alignment with Claude Code

Wave 1 and Wave 2 (partial) are complete:
- Plan 01: Inline Viewport Mode - DONE
- Plan 02: Timeline Render Model - DONE

Currently on Wave 2, about to start Plan 03 (Spinner Animation).

Branch: gsd/v0.4-subagents (has Phase 3.2 infrastructure)
All tests passing: 142 passed
</current_state>

<completed_work>

**Wave 1:**
- Plan 01: Inline Viewport Mode - DONE (commit: 3a1d618)
  - Replaced EnterAlternateScreen with Viewport::Inline(height)
  - Added calculate_inline_height with MIN_INLINE_HEIGHT=10, MARGIN_LINES=2
  - Added InputEvent enum (Key + Resize variants)
  - Updated session.rs to handle resize events
  - All terminal tests pass

**Wave 2:**
- Plan 02: Timeline Render Model - DONE (commits: 169dd95, b57c82d)
  - Created theme.rs with CliTheme struct (semantic colors)
  - Refactored timeline_text() to timeline_lines() -> Vec<Line<'static>>
  - Simplified message format (removed [turn_id] prefixes)
  - Merged Plan 04 into this plan (same render path)
  - Updated test assertions for new format
</completed_work>

<remaining_work>

**Wave 2 (continued):**
- Plan 03: Spinner Animation - NOT STARTED
  - Task 1: Create spinner.rs with ASCII flower animation
  - Task 2: Integrate spinner into CliApp status line
  - Task 3: Export spinner module from cli/mod.rs

**Wave 3:**
- Plan 05: Enhanced Banner - NOT STARTED
- Plan 06: Todo Auto-Collapse - NOT STARTED

**Wave 4:**
- Plan 07: Token Display - NOT STARTED
</remaining_work>

<decisions_made>

- Decided to use Viewport::Inline instead of alternate screen to preserve conversation history in terminal scrollback
- Decided to use Vec<Line> instead of String for timeline to enable semantic per-message coloring
- Merged Plans 02 and 04 together because they share the same render path dependency on Vec<Line>
- Plan 04 marked as superseded in its PLAN.md file
</decisions_made>

<blockers>
None - all tests passing, ready to continue with Plan 03.
</blockers>

<context>
Phase 3.2.1 aligns the TUI experience with Claude Code CLI:
1. Viewport::Inline - preserves history in terminal scrollback
2. Semantic colors - different message types have different colors
3. Spinner animation - ASCII flower frames during assistant activity
4. Simplified format - clean arrow prefixes (>>>, <<<, [...])
5. Enhanced banner - version, model, path info panel
6. Todo auto-collapse - hide when complete, add to history
7. Token display - show input/output token counts

Current codebase structure (src/cli/):
- app.rs - CliApp with timeline_lines(), theme integration
- terminal.rs - TerminalGuard with Viewport::Inline
- theme.rs - CliTheme with semantic colors
- session.rs - Session with InputEvent handling
- mod.rs - Module exports

Key dependencies already added:
- ratatui (with Viewport::Inline support)
- unicode-width (for text width calculations)
</context>

<next_action>
Start with Plan 03 (Spinner Animation):

1. Read .planning/phases/03.2.1-tui-alignment-with-claude-code/3.2.1-03-PLAN.md
2. Create src/cli/spinner.rs with:
   - SPINNER_FRAMES constant: ["·", "✻", "✽", "✶", "✳", "✢"]
   - FUN_MESSAGES constant: ["Thinking...", "Sketching...", "Reticulating...", ...]
   - Spinner struct with frame(), message(), status_text() methods
3. Add spinner field to CliApp
4. Update status_line() to show spinner when is_active
5. Export spinner module from cli/mod.rs
</next_action>
